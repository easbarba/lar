#!/usr/bin/env bash

set -euo pipefail

sudo su - postgres -c "createuser easbarbosa"
sudo su - postgres -c "createdb easbarbosadb`"

sudo -u postgres psql

# GRANT ALL PRIVILEGES ON DATABASE easbarbosadb TO easbarbosa;
