#!/usr/bin/env bash

# Debug Options
set -euo pipefail

COMMAND="$1"
shift 1

ARGUMENTS=("$@")

for arg in "${ARGUMENTS[@]}"
do
    echo -e "\n \e[42m $arg \e[0m \n" # white/green
    $COMMAND "$arg" # do not quote $COMMAND
done
