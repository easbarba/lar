#!/usr/bin/env bash

# custom emacs build
CUSTOM_EMACS="$HOME/.local/bin/emacs"
[[ -x $CUSTOM_EMACS ]] && exec $CUSTOM_EMACS

# emacs server
# [[ $(pgrep emacs) ]] && emacsclient -c

# distro shipped emacs build
[[ -x $(command -v emacs) ]] && emacs
