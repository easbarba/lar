#!/usr/bin/env bash

# Debug Options
set -euo pipefail

DEPS=(vagrant)
s-checkdeps "${DEPS[@]}"

[[ ! -x $(command -v vagrant) ]] && echo 'Vagrant is not installed!' && return

command='vagrant box add'
boxes=(debian/bullseye64 ubuntu/focal64)
s-dolist "$command" "${boxes[@]}"
