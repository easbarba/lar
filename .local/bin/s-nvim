#!/usr/bin/env bash

set -euo pipefail

DEPS=(nvim)
s-checkdeps "${DEPS[@]}"

NVIM_HOME="$HOME/.config/nvim"

grab() { git clone https://github.com/luan/nvim "$NVIM_HOME"; }

# * RUN
[[ ! -f "$NVIM_HOME"/init.lua ]] && grab
