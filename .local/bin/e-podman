#!/usr/bin/env bash
set -euo pipefail

e-echo "Podman - Rootless Containers"

_podman() {
    # Set subuid and subgid
    sudo touch /etc/subuid /etc/subgid
    sudo usermod --add-subuids 100000-165535 --add-subgids 100000-165535 "$USER"

    sudo podman system migrate
}

_podman
