#!/usr/bin/env bash

# Debug Options
set -euo pipefail

DEPS=(ruby gem)
DEV=(bundler rake solargraph)

e_checkdeps "${DEPS[@]}"

_install() { e_dolist "gem install" "${DEV[@]}"; }
_update() { e_dolist "gem update" "${DEV[@]}"; }

# * RUN
_install
_update
