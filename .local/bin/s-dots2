#!/usr/bin/env swift

import Foundation

let fileManager = FileManager.default
try fileManager.createSymbolicLink(atPath: "\(NSHomeDirectory())/.BASHRC",
                                   withDestinationPath: "\(NSHomeDirectory())/.bashrc")
