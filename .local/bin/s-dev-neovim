#!/usr/bin/env bash

# debug options
set -euo pipefail

DEPS=(nvim)
s-checkdeps "${DEPS[@]}"

NVIM_HOME="$HOME/.config/nvim"

[[ -d $NVIM_HOME ]] && rm -rf $NVIM_HOME

git clone https://github.com/luan/nvim "$NVIM_HOME"
