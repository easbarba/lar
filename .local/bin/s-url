#!/usr/bin/env bash

# Debug Options
set -euo pipefail
URL=''

[[ -x $(command -v xclip) ]] && URL=$(xclip -selection clipboard -o)
[[ -x $(command -v wl-paste) ]] && URL=$(wl-paste)

printf "%s" $URL
