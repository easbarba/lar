#!/usr/bin/env bash

FOLDER="$HOME/Pictures/wallpapers"
WALL="$(find "$FOLDER" -type f | shuf -n 1)"

echo "wallpaper: $WALL"
# if [[ -n $WAYLAND_DISPLAY ]]; then
#     swaybg -m fit -i "$WALL" &
# else
#     feh --bg-max "$WALL" &
# fi
