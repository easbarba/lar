#!/usr/bin/env bash

# Debug Options
set -euo pipefail

# Description: Get video

DEPS=(youtube-dl ffmpeg)
e_checkdeps "${DEPS[@]}"

FOLDER="$HOME/Videos"

# FOLDER
[[ ! -d $FOLDER ]] && mkdir "$FOLDER"
cd "$FOLDER"

# YOUTUBE-DL

GETTER='youtube-dl'
EXT='mp4'
# QUALITY='bestvideo+bestaudio'
FORMAT="--format $EXT"
OUTPUT='%(title)s.f%(format_id)s.%(ext)s'

# RUN
"$GETTER" --format "$EXT" --output "$OUTPUT" "$(e_url)"
