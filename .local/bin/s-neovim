#!/usr/bin/env bash

set -euo pipefail

DEPS=(nvim)
s-checkdeps "${DEPS[@]}"

NVIM_HOME="$HOME/.config/nvim"

cleanup() {
    rm -rf $NVIM_HOME
}

grab() {
    git clone https://github.com/luan/nvim "$NVIM_HOME"
}

# * RUN
cleanup
grab
