#!/usr/bin/env ruby
# frozen_string_literal: true

require 'taglib'
require 'pathname'
require 'optparse'

TITLE = lambda do |info, file|
  info = Pathname.new(file).basename('.*').to_path if info.empty?
  TagLib::FileRef.open(file) do |t|
    t.tag.title = info
    t.save
  end
end

ARTIST = lambda do |info, file|
  TagLib::FileRef.open(file) do |t|
    t.tag.artist = info
    t.save
  end
end

ALBUM = lambda do |info|
  TagLib::FileRef.open(file) do |t|
    t.tag.album = info
    t.save
  end
end

def run(it, info, func)
  raise 'FILE NOT FOUND' unless it.exist?

  if it.directory?
    it.children.each { |file| func.call(info, file.to_path) }
  else
    func.call(info, it.to_path)
  end
end

option_parser = OptionParser.new do |opts|
  opts.banner = 'Usage: tagedit [options]'
  it = String.new

  opts.on('-fFILE', '--file=FILE', 'file to edit') do |file|
    it = Pathname.new(file)
  end

  opts.on('-tTITLE', '--tittle=TITLE', 'edit title tag') do |title|
    run(it, title, TITLE)
  end

  opts.on('-aNAME', '--album=NAME', 'edit album tag') do |info|
    run(it, info, ALBUM)
  end

  opts.on('-sNAME', '--artist=NAME', 'edit artist tag') do |info|
    run(it, info, ARTIST)
  end
end

option_parser.parse! ['--help'] if ARGV.empty?
option_parser.parse!
