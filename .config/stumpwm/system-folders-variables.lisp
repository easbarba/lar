;; ============================================================
;; FOLDERS LOCATIONS

(defparameter *music*
  (concatenate 'string *home* "Music")
  "Tunes Folder.")

(defparameter *books*
  (concatenate 'string *home* "Books")
  "Books Folder.")

(defparameter *pictures*
  (concatenate 'string *home* "Pictures")
  "Fotografias Folder.")

(defparameter *downloads*
  (concatenate 'string *home* "Downloads")
  "Downloads Folder.")

(defparameter *local-bin*
  (concatenate 'string *home* ".local/bin")
  "Local bin Folder.")
