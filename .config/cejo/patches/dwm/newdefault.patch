From 4c8c406034d6ce2e400efb89f3ac97c750b56b22 Mon Sep 17 00:00:00 2001
From: easbarbosa <easbarbosa@tutanota.me>
Date: Fri, 30 Jul 2021 14:22:34 -0300
Subject: [PATCH 01/10] new functions and bindings

---
 config.def.h | 209 +++++++++++++++++++++++++++++----------------------
 1 file changed, 121 insertions(+), 88 deletions(-)

diff --git a/config.def.h b/config.def.h
index 1c0b587..61fde86 100644
--- a/config.def.h
+++ b/config.def.h
@@ -1,115 +1,148 @@
 /* See LICENSE file for copyright and license details. */
 
 /* appearance */
-static const unsigned int borderpx  = 1;        /* border pixel of windows */
-static const unsigned int snap      = 32;       /* snap pixel */
-static const int showbar            = 1;        /* 0 means no bar */
-static const int topbar             = 1;        /* 0 means bottom bar */
-static const char *fonts[]          = { "monospace:size=10" };
-static const char dmenufont[]       = "monospace:size=10";
-static const char col_gray1[]       = "#222222";
-static const char col_gray2[]       = "#444444";
-static const char col_gray3[]       = "#bbbbbb";
-static const char col_gray4[]       = "#eeeeee";
-static const char col_cyan[]        = "#005577";
-static const char *colors[][3]      = {
-	/*               fg         bg         border   */
-	[SchemeNorm] = { col_gray3, col_gray1, col_gray2 },
-	[SchemeSel]  = { col_gray4, col_cyan,  col_cyan  },
+static const unsigned int borderpx = 1; /* border pixel of windows */
+static const unsigned int snap = 32;    /* snap pixel */
+static const int showbar = 1;           /* 0 means no bar */
+static const int topbar = 1;            /* 0 means bottom bar */
+static const char *fonts[] = {"monospace:size=9"};
+static const char dmenufont[] = "monospace:size=9";
+static const char col_gray1[] = "#222222";
+static const char col_gray2[] = "#444444";
+static const char col_gray3[] = "#bbbbbb";
+static const char col_gray4[] = "#eeeeee";
+static const char col_cyan[] = "#005577";
+static const char *colors[][3] = {
+    /*               fg         bg         border   */
+    [SchemeNorm] = {col_gray3, col_gray1, col_gray2},
+    [SchemeSel] = {col_gray4, col_cyan, col_cyan},
 };
 
 /* tagging */
-static const char *tags[] = { "1", "2", "3", "4", "5", "6", "7", "8", "9" };
+static const char *tags[] = {"mx", "read", "term", "media", "www"};
 
 static const Rule rules[] = {
-	/* xprop(1):
-	 *	WM_CLASS(STRING) = instance, class
-	 *	WM_NAME(STRING) = title
-	 */
-	/* class      instance    title       tags mask     isfloating   monitor */
-	{ "Gimp",     NULL,       NULL,       0,            1,           -1 },
-	{ "Firefox",  NULL,       NULL,       1 << 8,       0,           -1 },
+    /* xprop(1):
+     *	WM_CLASS(STRING) = instance, class
+     *	WM_NAME(STRING) = title
+     */
+    /* class      instance    title       tags mask     isfloating   monitor */
+    {"Gimp", NULL, NULL, 0, 1, -1},
+    {"Firefox", NULL, NULL, 1 << 4, 0, -1},
+
 };
 
 /* layout(s) */
-static const float mfact     = 0.55; /* factor of master area size [0.05..0.95] */
-static const int nmaster     = 1;    /* number of clients in master area */
-static const int resizehints = 1;    /* 1 means respect size hints in tiled resizals */
+static const float mfact = 0.55; /* factor of master area size [0.05..0.95] */
+static const int nmaster = 1;    /* number of clients in master area */
+static const int resizehints =
+    1; /* 1 means respect size hints in tiled resizals */
+static const int lockfullscreen =
+    1; /* 1 will force focus on the fullscreen window */
 
 static const Layout layouts[] = {
-	/* symbol     arrange function */
-	{ "[]=",      tile },    /* first entry is default */
-	{ "><>",      NULL },    /* no layout function means floating behavior */
-	{ "[M]",      monocle },
+    /* symbol     arrange function */
+    {"[]=", tile}, /* first entry is default */
+    {"><>", NULL}, /* no layout function means floating behavior */
+    {"[M]", monocle},
 };
 
 /* key definitions */
-#define MODKEY Mod1Mask
-#define TAGKEYS(KEY,TAG) \
-	{ MODKEY,                       KEY,      view,           {.ui = 1 << TAG} }, \
-	{ MODKEY|ControlMask,           KEY,      toggleview,     {.ui = 1 << TAG} }, \
-	{ MODKEY|ShiftMask,             KEY,      tag,            {.ui = 1 << TAG} }, \
-	{ MODKEY|ControlMask|ShiftMask, KEY,      toggletag,      {.ui = 1 << TAG} },
+#define MODKEY Mod4Mask
+#define TAGKEYS(KEY, TAG)                                                      \
+  {MODKEY, KEY, view, {.ui = 1 << TAG}},                                       \
+      {MODKEY | ControlMask, KEY, toggleview, {.ui = 1 << TAG}},               \
+      {MODKEY | ShiftMask, KEY, tag, {.ui = 1 << TAG}},                        \
+      {MODKEY | ControlMask | ShiftMask, KEY, toggletag, {.ui = 1 << TAG}},
 
 /* helper for spawning shell commands in the pre dwm-5.0 fashion */
-#define SHCMD(cmd) { .v = (const char*[]){ "/bin/sh", "-c", cmd, NULL } }
+#define SHCMD(cmd)                                                             \
+  {                                                                            \
+    .v = (const char *[]) { "/bin/sh", "-c", cmd, NULL }                       \
+  }
 
 /* commands */
-static char dmenumon[2] = "0"; /* component of dmenucmd, manipulated in spawn() */
-static const char *dmenucmd[] = { "dmenu_run", "-m", dmenumon, "-fn", dmenufont, "-nb", col_gray1, "-nf", col_gray3, "-sb", col_cyan, "-sf", col_gray4, NULL };
-static const char *termcmd[]  = { "st", NULL };
+static char dmenumon[2] =
+    "0"; /* component of dmenucmd, manipulated in spawn() */
+static const char *dmenucmd[] = {
+    "dmenu_run", "-m",      dmenumon, "-fn",    dmenufont, "-nb",     col_gray1,
+    "-nf",       col_gray3, "-sb",    col_cyan, "-sf",     col_gray4, NULL};
+static const char *termcmd[] = {"st", NULL};
+
+/* User commands  */
+static const char *voldown[] = {"gota", "volume", "down" NULL};
+static const char *volup[] = {"gota", "volume", "up" NULL};
+static const char *voltoggle[] = {"gota", "volume", "toggle" NULL};
+static const char *playvideo[] = {"e_play" NULL};
+static const char *printfull[] = {"gota", "screen", "full" NULL};
+static const char *printpartial[] = {"gota", "screen", "partial" NULL};
+static const char *playertoggle[] = {"mpc", "toggle" NULL};
+static const char *playerup[] = {"mpc", "volume", "+3" NULL};
+static const char *playerdown[] = {"mpc", "volume", "-3" NULL};
+static const char *mediatoggle[] = {"playerctl", "play-pause" NULL};
 
 static Key keys[] = {
-	/* modifier                     key        function        argument */
-	{ MODKEY,                       XK_p,      spawn,          {.v = dmenucmd } },
-	{ MODKEY|ShiftMask,             XK_Return, spawn,          {.v = termcmd } },
-	{ MODKEY,                       XK_b,      togglebar,      {0} },
-	{ MODKEY,                       XK_j,      focusstack,     {.i = +1 } },
-	{ MODKEY,                       XK_k,      focusstack,     {.i = -1 } },
-	{ MODKEY,                       XK_i,      incnmaster,     {.i = +1 } },
-	{ MODKEY,                       XK_d,      incnmaster,     {.i = -1 } },
-	{ MODKEY,                       XK_h,      setmfact,       {.f = -0.05} },
-	{ MODKEY,                       XK_l,      setmfact,       {.f = +0.05} },
-	{ MODKEY,                       XK_Return, zoom,           {0} },
-	{ MODKEY,                       XK_Tab,    view,           {0} },
-	{ MODKEY|ShiftMask,             XK_c,      killclient,     {0} },
-	{ MODKEY,                       XK_t,      setlayout,      {.v = &layouts[0]} },
-	{ MODKEY,                       XK_f,      setlayout,      {.v = &layouts[1]} },
-	{ MODKEY,                       XK_m,      setlayout,      {.v = &layouts[2]} },
-	{ MODKEY,                       XK_space,  setlayout,      {0} },
-	{ MODKEY|ShiftMask,             XK_space,  togglefloating, {0} },
-	{ MODKEY,                       XK_0,      view,           {.ui = ~0 } },
-	{ MODKEY|ShiftMask,             XK_0,      tag,            {.ui = ~0 } },
-	{ MODKEY,                       XK_comma,  focusmon,       {.i = -1 } },
-	{ MODKEY,                       XK_period, focusmon,       {.i = +1 } },
-	{ MODKEY|ShiftMask,             XK_comma,  tagmon,         {.i = -1 } },
-	{ MODKEY|ShiftMask,             XK_period, tagmon,         {.i = +1 } },
-	TAGKEYS(                        XK_1,                      0)
-	TAGKEYS(                        XK_2,                      1)
-	TAGKEYS(                        XK_3,                      2)
-	TAGKEYS(                        XK_4,                      3)
-	TAGKEYS(                        XK_5,                      4)
-	TAGKEYS(                        XK_6,                      5)
-	TAGKEYS(                        XK_7,                      6)
-	TAGKEYS(                        XK_8,                      7)
-	TAGKEYS(                        XK_9,                      8)
-	{ MODKEY|ShiftMask,             XK_q,      quit,           {0} },
+    /* modifier                     key        function        argument */
+    {MODKEY, XK_p, spawn, {.v = dmenucmd}},
+    {MODKEY | ShiftMask, XK_Return, spawn, {.v = termcmd}},
+    {MODKEY, XK_b, togglebar, {0}},
+    {MODKEY, XK_j, focusstack, {.i = +1}},
+    {MODKEY, XK_k, focusstack, {.i = -1}},
+    {MODKEY, XK_i, incnmaster, {.i = +1}},
+    {MODKEY, XK_d, incnmaster, {.i = -1}},
+    {MODKEY, XK_h, setmfact, {.f = -0.05}},
+    {MODKEY, XK_l, setmfact, {.f = +0.05}},
+    {MODKEY, XK_Return, zoom, {0}},
+    {MODKEY, XK_Tab, view, {0}},
+    {MODKEY | ShiftMask, XK_c, killclient, {0}},
+    {MODKEY, XK_t, setlayout, {.v = &layouts[0]}},
+    {MODKEY, XK_f, setlayout, {.v = &layouts[1]}},
+    {MODKEY, XK_m, setlayout, {.v = &layouts[2]}},
+    {MODKEY, XK_space, setlayout, {0}},
+    {MODKEY | ShiftMask, XK_space, togglefloating, {0}},
+    {MODKEY, XK_0, view, {.ui = ~0}},
+    {MODKEY | ShiftMask, XK_0, tag, {.ui = ~0}},
+    {MODKEY, XK_comma, focusmon, {.i = -1}},
+    {MODKEY, XK_period, focusmon, {.i = +1}},
+    {MODKEY | ShiftMask, XK_comma, tagmon, {.i = -1}},
+    {MODKEY | ShiftMask, XK_period, tagmon, {.i = +1}},
+
+    /* Volume */
+    {MODKEY | ShiftMask, XK_e, spawn, {.v = voltoggle}},
+    {MODKEY | ShiftMask, XK_x, spawn, {.v = voldown}},
+    {MODKEY | ShiftMask, XK_w, spawn, {.v = volup}},
+
+    /* Media */
+    {MODKEY | ShiftMask, XK_p, spawn, {.v = playvideo}},
+    {MODKEY, XK_e, spawn, {.v = playertoggle}},
+    {MODKEY, XK_w, spawn, {.v = playerup}},
+    {MODKEY, XK_s, spawn, {.v = playerdown}},
+    {MODKEY, XK_s, spawn, {.v = mediatoggle}},
+
+    /* Print */
+    {MODKEY, XK_o, spawn, {.v = printfull}},
+    {MODKEY | ShiftMask, XK_o, spawn, {.v = printpartial}},
+
+    TAGKEYS(XK_1, 0) TAGKEYS(XK_2, 1) TAGKEYS(XK_3, 2) TAGKEYS(XK_4, 3)
+        TAGKEYS(XK_5, 4) TAGKEYS(XK_6, 5) TAGKEYS(XK_7, 6) TAGKEYS(XK_8, 7)
+            TAGKEYS(XK_9, 8){MODKEY | ShiftMask, XK_q, quit, {0}},
 };
 
 /* button definitions */
-/* click can be ClkTagBar, ClkLtSymbol, ClkStatusText, ClkWinTitle, ClkClientWin, or ClkRootWin */
+/* click can be ClkTagBar, ClkLtSymbol, ClkStatusText, ClkWinTitle,
+ * ClkClientWin, or ClkRootWin */
 static Button buttons[] = {
-	/* click                event mask      button          function        argument */
-	{ ClkLtSymbol,          0,              Button1,        setlayout,      {0} },
-	{ ClkLtSymbol,          0,              Button3,        setlayout,      {.v = &layouts[2]} },
-	{ ClkWinTitle,          0,              Button2,        zoom,           {0} },
-	{ ClkStatusText,        0,              Button2,        spawn,          {.v = termcmd } },
-	{ ClkClientWin,         MODKEY,         Button1,        movemouse,      {0} },
-	{ ClkClientWin,         MODKEY,         Button2,        togglefloating, {0} },
-	{ ClkClientWin,         MODKEY,         Button3,        resizemouse,    {0} },
-	{ ClkTagBar,            0,              Button1,        view,           {0} },
-	{ ClkTagBar,            0,              Button3,        toggleview,     {0} },
-	{ ClkTagBar,            MODKEY,         Button1,        tag,            {0} },
-	{ ClkTagBar,            MODKEY,         Button3,        toggletag,      {0} },
+    /* click                event mask      button          function argument
+     */
+    {ClkLtSymbol, 0, Button1, setlayout, {0}},
+    {ClkLtSymbol, 0, Button3, setlayout, {.v = &layouts[2]}},
+    {ClkWinTitle, 0, Button2, zoom, {0}},
+    {ClkStatusText, 0, Button2, spawn, {.v = termcmd}},
+    {ClkClientWin, MODKEY, Button1, movemouse, {0}},
+    {ClkClientWin, MODKEY, Button2, togglefloating, {0}},
+    {ClkClientWin, MODKEY, Button3, resizemouse, {0}},
+    {ClkTagBar, 0, Button1, view, {0}},
+    {ClkTagBar, 0, Button3, toggleview, {0}},
+    {ClkTagBar, MODKEY, Button1, tag, {0}},
+    {ClkTagBar, MODKEY, Button3, toggletag, {0}},
 };
-
-- 
2.32.0


From 2bbd31127ddab0798c4b071d5202e21097e1b335 Mon Sep 17 00:00:00 2001
From: easbarbosa <easbarbosa@tutanota.me>
Date: Fri, 30 Jul 2021 14:35:13 -0300
Subject: [PATCH 02/10] append lacking colon

---
 config.def.h | 20 ++++++++++----------
 1 file changed, 10 insertions(+), 10 deletions(-)

diff --git a/config.def.h b/config.def.h
index 61fde86..439d006 100644
--- a/config.def.h
+++ b/config.def.h
@@ -70,16 +70,16 @@ static const char *dmenucmd[] = {
 static const char *termcmd[] = {"st", NULL};
 
 /* User commands  */
-static const char *voldown[] = {"gota", "volume", "down" NULL};
-static const char *volup[] = {"gota", "volume", "up" NULL};
-static const char *voltoggle[] = {"gota", "volume", "toggle" NULL};
-static const char *playvideo[] = {"e_play" NULL};
-static const char *printfull[] = {"gota", "screen", "full" NULL};
-static const char *printpartial[] = {"gota", "screen", "partial" NULL};
-static const char *playertoggle[] = {"mpc", "toggle" NULL};
-static const char *playerup[] = {"mpc", "volume", "+3" NULL};
-static const char *playerdown[] = {"mpc", "volume", "-3" NULL};
-static const char *mediatoggle[] = {"playerctl", "play-pause" NULL};
+static const char *voldown[] = {"gota", "volume", "down", NULL};
+static const char *volup[] = {"gota", "volume", "up", NULL};
+static const char *voltoggle[] = {"gota", "volume", "toggle", NULL};
+static const char *playvideo[] = {"e_play", NULL};
+static const char *printfull[] = {"gota", "screen", "full", NULL};
+static const char *printpartial[] = {"gota", "screen", "partial", NULL};
+static const char *playertoggle[] = {"mpc", "toggle", NULL};
+static const char *playerup[] = {"mpc", "volume", "+3", NULL};
+static const char *playerdown[] = {"mpc", "volume", "-3", NULL};
+static const char *mediatoggle[] = {"playerctl", "play-pause", NULL};
 
 static Key keys[] = {
     /* modifier                     key        function        argument */
-- 
2.32.0


From f5c8dd690587b5c6b765728ae5820c1a9a777b7e Mon Sep 17 00:00:00 2001
From: easbarbosa <easbarbosa@tutanota.me>
Date: Fri, 30 Jul 2021 14:52:48 -0300
Subject: [PATCH 03/10] bar: all blackey

---
 config.def.h | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

diff --git a/config.def.h b/config.def.h
index 439d006..0479b54 100644
--- a/config.def.h
+++ b/config.def.h
@@ -11,7 +11,7 @@ static const char col_gray1[] = "#222222";
 static const char col_gray2[] = "#444444";
 static const char col_gray3[] = "#bbbbbb";
 static const char col_gray4[] = "#eeeeee";
-static const char col_cyan[] = "#005577";
+static const char col_cyan[] = "#222222";
 static const char *colors[][3] = {
     /*               fg         bg         border   */
     [SchemeNorm] = {col_gray3, col_gray1, col_gray2},
-- 
2.32.0


From 94e209947471f65f570af2403dc3a7440b2dad57 Mon Sep 17 00:00:00 2001
From: easbarbosa <easbarbosa@tutanota.me>
Date: Fri, 30 Jul 2021 14:59:14 -0300
Subject: [PATCH 04/10] add some apps to rule

---
 config.def.h | 7 +++++--
 1 file changed, 5 insertions(+), 2 deletions(-)

diff --git a/config.def.h b/config.def.h
index 0479b54..5b1fb5a 100644
--- a/config.def.h
+++ b/config.def.h
@@ -28,8 +28,11 @@ static const Rule rules[] = {
      */
     /* class      instance    title       tags mask     isfloating   monitor */
     {"Gimp", NULL, NULL, 0, 1, -1},
-    {"Firefox", NULL, NULL, 1 << 4, 0, -1},
-
+    {"Firefox", NULL, NULL, 1 << 6, 0, -1},
+    {"st-256color", NULL, NULL, 4 << 4, 0, -1},
+    {"Atril", NULL, NULL, 2 << 4, 0, -1},
+    {"mpv", NULL, NULL, 2 << 5, 0, -1},
+    {"Emacs", NULL, NULL, 1 << 0, 0, -1},
 };
 
 /* layout(s) */
-- 
2.32.0


From d8ce63bf06b6cac27f9ee8fe80da836c4f2150a1 Mon Sep 17 00:00:00 2001
From: easbarbosa <easbarbosa@tutanota.me>
Date: Fri, 30 Jul 2021 15:02:33 -0300
Subject: [PATCH 05/10] more rules

---
 config.def.h | 8 ++++----
 1 file changed, 4 insertions(+), 4 deletions(-)

diff --git a/config.def.h b/config.def.h
index 5b1fb5a..40aca6d 100644
--- a/config.def.h
+++ b/config.def.h
@@ -28,10 +28,10 @@ static const Rule rules[] = {
      */
     /* class      instance    title       tags mask     isfloating   monitor */
     {"Gimp", NULL, NULL, 0, 1, -1},
-    {"Firefox", NULL, NULL, 1 << 6, 0, -1},
-    {"st-256color", NULL, NULL, 4 << 4, 0, -1},
-    {"Atril", NULL, NULL, 2 << 4, 0, -1},
-    {"mpv", NULL, NULL, 2 << 5, 0, -1},
+    {"Firefox", NULL, NULL, 1 << 5, 0, -1},
+    {"st-256color", NULL, NULL, 1 << 3, 0, -1},
+    {"Atril", NULL, NULL, 1 << 1, 0, -1},
+    {"mpv", NULL, NULL, 1 << 4, 0, -1},
     {"Emacs", NULL, NULL, 1 << 0, 0, -1},
 };
 
-- 
2.32.0


From 5ac1933c559a19a9d52fce068fdf5b71fe062b03 Mon Sep 17 00:00:00 2001
From: easbarbosa <easbarbosa@tutanota.me>
Date: Fri, 30 Jul 2021 15:04:43 -0300
Subject: [PATCH 06/10] rules in order ascendent

---
 config.def.h | 10 +++++-----
 1 file changed, 5 insertions(+), 5 deletions(-)

diff --git a/config.def.h b/config.def.h
index 40aca6d..07a9360 100644
--- a/config.def.h
+++ b/config.def.h
@@ -27,12 +27,12 @@ static const Rule rules[] = {
      *	WM_NAME(STRING) = title
      */
     /* class      instance    title       tags mask     isfloating   monitor */
-    {"Gimp", NULL, NULL, 0, 1, -1},
-    {"Firefox", NULL, NULL, 1 << 5, 0, -1},
-    {"st-256color", NULL, NULL, 1 << 3, 0, -1},
-    {"Atril", NULL, NULL, 1 << 1, 0, -1},
-    {"mpv", NULL, NULL, 1 << 4, 0, -1},
     {"Emacs", NULL, NULL, 1 << 0, 0, -1},
+    {"Atril", NULL, NULL, 1 << 1, 0, -1},
+    {"st-256color", NULL, NULL, 1 << 2, 0, -1},
+    {"Gimp", NULL, NULL, 1 << 3, 1, -1},
+    {"mpv", NULL, NULL, 1 << 3, 0, -1},
+    {"Firefox", NULL, NULL, 1 << 4, 0, -1},
 };
 
 /* layout(s) */
-- 
2.32.0


From ddf1af0fca5e532bd80b8860c8b3d4f931ca6ab3 Mon Sep 17 00:00:00 2001
From: easbarbosa <easbarbosa@tutanota.me>
Date: Fri, 30 Jul 2021 15:05:54 -0300
Subject: [PATCH 07/10] rule: fix firefox string

---
 config.def.h | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

diff --git a/config.def.h b/config.def.h
index 07a9360..184180b 100644
--- a/config.def.h
+++ b/config.def.h
@@ -32,7 +32,7 @@ static const Rule rules[] = {
     {"st-256color", NULL, NULL, 1 << 2, 0, -1},
     {"Gimp", NULL, NULL, 1 << 3, 1, -1},
     {"mpv", NULL, NULL, 1 << 3, 0, -1},
-    {"Firefox", NULL, NULL, 1 << 4, 0, -1},
+    {"firefox", NULL, NULL, 1 << 4, 0, -1},
 };
 
 /* layout(s) */
-- 
2.32.0


From 989e7da74c591029512d2e4c07236c18d114b365 Mon Sep 17 00:00:00 2001
From: easbarbosa <easbarbosa@tutanota.me>
Date: Fri, 30 Jul 2021 15:48:55 -0300
Subject: [PATCH 08/10] fix volume bindings

---
 config.def.h | 4 ++--
 1 file changed, 2 insertions(+), 2 deletions(-)

diff --git a/config.def.h b/config.def.h
index 184180b..5e56284 100644
--- a/config.def.h
+++ b/config.def.h
@@ -112,15 +112,15 @@ static Key keys[] = {
 
     /* Volume */
     {MODKEY | ShiftMask, XK_e, spawn, {.v = voltoggle}},
-    {MODKEY | ShiftMask, XK_x, spawn, {.v = voldown}},
     {MODKEY | ShiftMask, XK_w, spawn, {.v = volup}},
+    {MODKEY | ShiftMask, XK_s, spawn, {.v = voldown}},
 
     /* Media */
     {MODKEY | ShiftMask, XK_p, spawn, {.v = playvideo}},
     {MODKEY, XK_e, spawn, {.v = playertoggle}},
     {MODKEY, XK_w, spawn, {.v = playerup}},
     {MODKEY, XK_s, spawn, {.v = playerdown}},
-    {MODKEY, XK_s, spawn, {.v = mediatoggle}},
+    {MODKEY, XK_z, spawn, {.v = mediatoggle}},
 
     /* Print */
     {MODKEY, XK_o, spawn, {.v = printfull}},
-- 
2.32.0


From e371ad06e169d53b24c28b4615be7a0f25c58b29 Mon Sep 17 00:00:00 2001
From: easbarbosa <easbarbosa@tutanota.me>
Date: Fri, 30 Jul 2021 15:55:03 -0300
Subject: [PATCH 09/10] add slock binding

---
 config.def.h | 4 ++++
 1 file changed, 4 insertions(+)

diff --git a/config.def.h b/config.def.h
index 5e56284..861e7ac 100644
--- a/config.def.h
+++ b/config.def.h
@@ -83,6 +83,7 @@ static const char *playertoggle[] = {"mpc", "toggle", NULL};
 static const char *playerup[] = {"mpc", "volume", "+3", NULL};
 static const char *playerdown[] = {"mpc", "volume", "-3", NULL};
 static const char *mediatoggle[] = {"playerctl", "play-pause", NULL};
+static const char *wmlock[] = {"slock", NULL};
 
 static Key keys[] = {
     /* modifier                     key        function        argument */
@@ -126,6 +127,9 @@ static Key keys[] = {
     {MODKEY, XK_o, spawn, {.v = printfull}},
     {MODKEY | ShiftMask, XK_o, spawn, {.v = printpartial}},
 
+    /* Utils */
+    {MODKEY, XK_c, spawn, {.v = wmlock}},
+
     TAGKEYS(XK_1, 0) TAGKEYS(XK_2, 1) TAGKEYS(XK_3, 2) TAGKEYS(XK_4, 3)
         TAGKEYS(XK_5, 4) TAGKEYS(XK_6, 5) TAGKEYS(XK_7, 6) TAGKEYS(XK_8, 7)
             TAGKEYS(XK_9, 8){MODKEY | ShiftMask, XK_q, quit, {0}},
-- 
2.32.0


From 275be167ca42e5d8d0cf5e8087ac062e36a92b35 Mon Sep 17 00:00:00 2001
From: easbarbosa <easbarbosa@tutanota.me>
Date: Fri, 30 Jul 2021 16:38:10 -0300
Subject: [PATCH 10/10] do not show bar & disable resize hint

---
 config.def.h | 4 ++--
 1 file changed, 2 insertions(+), 2 deletions(-)

diff --git a/config.def.h b/config.def.h
index 861e7ac..6fe4a53 100644
--- a/config.def.h
+++ b/config.def.h
@@ -3,7 +3,7 @@
 /* appearance */
 static const unsigned int borderpx = 1; /* border pixel of windows */
 static const unsigned int snap = 32;    /* snap pixel */
-static const int showbar = 1;           /* 0 means no bar */
+static const int showbar = 0;           /* 0 means no bar */
 static const int topbar = 1;            /* 0 means bottom bar */
 static const char *fonts[] = {"monospace:size=9"};
 static const char dmenufont[] = "monospace:size=9";
@@ -39,7 +39,7 @@ static const Rule rules[] = {
 static const float mfact = 0.55; /* factor of master area size [0.05..0.95] */
 static const int nmaster = 1;    /* number of clients in master area */
 static const int resizehints =
-    1; /* 1 means respect size hints in tiled resizals */
+    0; /* 1 means respect size hints in tiled resizals */
 static const int lockfullscreen =
     1; /* 1 will force focus on the fullscreen window */
 
-- 
2.32.0

