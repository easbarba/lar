{
    "task": "systemd-user",
    "command": "systemctl enable --now --user",
    "jobs": {
        "music": [
            "mpd",
            "wireplumber",
            "pipewire",
            "pipewire-pulse"
        ]
    }
}
