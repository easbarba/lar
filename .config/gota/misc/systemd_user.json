{
    "task": "systemd-user",
    "command": "systemctl enable --now --user",
    "jobs": {
        "music": [
            "wireplumber",
            "pipewire",
            "pipewire-pulse"
        ]
    }
}
