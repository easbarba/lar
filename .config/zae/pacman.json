{
    "exec": "/usr/bin/pacman",
    "super": {
        "clean": "--sync --clean",
        "deps": "",
        "download": "--sync --downloadonly",
        "install": "--sync",
        "remove": "--remove",
        "update": "--sync --refresh",
        "upgrade": "--sync --refresh --sysupgrade"
    },
    "user": {
        "depends": "--sync --info",
        "search": "--sync --search",
        "help": "help",
        "info": "--sync --info",
        "installed": "--query"
    }
}
